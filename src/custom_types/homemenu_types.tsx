export namespace IHomeMenu {
    export interface HomeMenuProps {
        anchorId: string;
        headerTitle: string;
        renderItems: string[];
        customDiv: React.ReactFragment[];
    }
}
